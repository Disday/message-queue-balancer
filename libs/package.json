{
  "name": "@mqb/libs",
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "pnpm clean && tsc --incremental",
    "start:dev": "tsc -w --incremental",
    "build:watch": "tsc -w --preserveWatchOutput"
  }
}